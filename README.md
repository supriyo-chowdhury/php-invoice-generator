# php-invoice-generator
A professional GST-ready invoice generator system built in PHP that dynamically generates styled PDF invoices using Dompdf. This project demonstrates real-world billing logic, tax calculations, and PDF rendering with custom UI design.
